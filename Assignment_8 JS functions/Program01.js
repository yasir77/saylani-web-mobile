// 1.  write a javascript program that gets temperature from user in celsius and convert it into
// Fahrenheit using the formula F = 9/5 * C + 32, alert the output.

var tempInC = prompt("ENTER TEMPRATURE IN CELSIUS");
var tempInF = (9 / 5) * parseInt(tempInC) + 32;
alert(tempInF);



