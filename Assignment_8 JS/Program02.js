// 2.  write a javascript program that gets person height from user in inches and convert it into 
// centimeters using the formula heighInCentimeters = 2.54 * height, console the output.

var heightInInches = prompt("ENTER HEIGHT IN INCHES");
var heightInCentimeters = 2.54 * parseInt(heightInInches);
console.log(heightInCentimeters);